{
  "name": "twineo",
  "version": "0.4.0",
  "private": true,
  "scripts": {
    "build:css": "postcss ./templates/styles.css -o ./public/styles.min.css",
    "build:front": "npm --prefix front ci --include=optional && npm --prefix front run build",
    "build:assets": "npm run build:front && npm run build:css && node scripts/sync_front_assets.mjs",
    "start": "cargo run"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "daisyui": "^3.1.5",
    "postcss": "^8.4.24",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.3.2"
  }
}
